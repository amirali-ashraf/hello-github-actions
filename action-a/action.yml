name: "Hello Actions"
description: "Greet someone"
author: "someone@somewhere.ca"

inputs:
  MY_NAME:
    description: "Someone to greet"
    required: true
    default: "World"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"

